<header>
  <div class="container">
    <nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light">
      <%= link_to image_tag(""), root_path, class:"navbar-brand" %></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
          <%= link_to t("static_page.home.title"), root_path %>
          </li>
          <li class="nav-item">
            <%= render "books/search" %>
          </li>

        </ul>

        <ul class="nav justify-content-end">
          <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to t("users.upload"), new_book_path %>
          </li>

          <li class="nav-item">
            <%= link_to t("users.logout.title"), destroy_user_session_path, method: :delete %>
          </li>

          <li class="nav-item">
            <%= render "users/shared/user_minimize"%>
          </li>

          <% else %>
          <li class="nav-item">
            <%= link_to t("users.login.title"), new_user_session_path %></li>
          <li class="nav-item">
            <%= link_to t("users.signup.title"), new_user_registration_path %>
          </li>
        <% end %>
        </ul>
      </div>
    </nav>
  </div>
</header>
